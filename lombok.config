# Lombok 1.18.40+ no longer copies Jackson annotations from fields to accessors by default.
# Without this, fields like `aInt` + @JsonProperty("a-int") serialize as both "aint" and "a-int".
config.stopBubbling = true
lombok.copyJacksonAnnotationsToAccessors = true
